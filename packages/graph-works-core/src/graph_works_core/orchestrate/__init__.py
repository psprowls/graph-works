"""The orchestrate vertical: the stage-advance shell around
`workspace.pipeline`'s variant table. Layer 2 — independent of every other
vertical; imports only `workspace`.
"""

from __future__ import annotations
