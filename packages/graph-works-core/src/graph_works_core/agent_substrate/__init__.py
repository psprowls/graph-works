"""The agent-facing leaves: the tool loop, catalog/citation tool functions,
and the role-to-model binding. Layer 1, shared — every vertical may import
it; it imports only `workspace`.
"""

from __future__ import annotations
