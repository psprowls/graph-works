---
title: Design spec — beta feature
category: work
summary: The design spec feature-beta records in sources[].
---

# Design spec — beta feature

An artifact, not a concept the work lane reads. `IGNORE` keeps it out of
`bundle.concepts`, and `has_member` still finds it.
