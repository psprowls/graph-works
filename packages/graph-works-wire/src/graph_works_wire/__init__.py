"""graph-works-wire: every typed graph-works result, projected to plain JSON data.

One copy of each projection, so every interface -- `gw --json` today,
`graph-works-serve` next -- emits the same shape. Pure: no interface
framework, no I/O, no encoding. See this package's AGENTS.md.
"""
