frontmatter:
  owned: [ecosystem, version]
  provenance: [generated, last_updated_commit, tokens, prose_refreshed_commit, prose_refresh_attempts]
sections:
  - heading: Purpose
    required: true
    placeholder: |
      > TODO: what this agent plugin does and who uses it, in one paragraph.
  - heading: Commands
    ownership: generated
    required: true
    placeholder: |
      _(populated by `code-wiki-okf sync` — not yet generated)_
  - heading: Agents
    ownership: generated
    required: true
    placeholder: |
      _(populated by `code-wiki-okf sync` — not yet generated)_
  - heading: Skills
    ownership: generated
    required: true
    placeholder: |
      _(populated by `code-wiki-okf sync` — not yet generated)_
  - heading: Scripts
    ownership: generated
    required: true
    placeholder: |
      _(populated by `code-wiki-okf sync` — not yet generated)_
  - heading: Hooks
    ownership: generated
    required: true
    placeholder: |
      _(populated by `code-wiki-okf sync` — not yet generated)_
  - heading: MCP servers
    ownership: generated
    required: true
    placeholder: |
      _(populated by `code-wiki-okf sync` — not yet generated)_
  - heading: How it fits together
    placeholder: |
      > TODO: the inferred cross-component relationships.
