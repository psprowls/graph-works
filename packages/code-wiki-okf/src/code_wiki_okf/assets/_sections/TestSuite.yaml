frontmatter:
  owned: [tested_packages, suite_kind, file_count]
  provenance: [generated, last_updated_commit, tokens, prose_refreshed_commit, prose_refresh_attempts]
sections:
  - heading: Purpose
    required: true
    placeholder: |
      > TODO: what this suite tests and why, in one paragraph.
  - heading: How to run
    placeholder: |
      > TODO: the exact command(s) to run this suite.
  - heading: Test conventions
    placeholder: |
      > TODO: the naming, structure, and style conventions this suite follows.
  - heading: Fixtures
    placeholder: |
      > TODO: the shared fixtures this suite relies on, and where they live.
  - heading: Files
    ownership: generated
    required: true
    placeholder: |
      _(populated by `code-wiki-okf sync` — not yet generated)_
