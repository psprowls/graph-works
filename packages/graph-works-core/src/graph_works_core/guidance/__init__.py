"""Deterministic guidance inputs: the claims index and the `affects` closure.

A layer of its own, below every vertical and above the shared substrate, so
the `work` vertical's guidance assembly can import it.
"""

from __future__ import annotations
