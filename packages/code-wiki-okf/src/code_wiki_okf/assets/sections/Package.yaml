frontmatter:
  owned: [language, version, depends_on, test_suites, entry_points, used_by, versions_in_use]
  provenance: [generated, last_updated_commit, tokens, prose_refreshed_commit, prose_refresh_attempts]
sections:
  - heading: Purpose
    required: true
    audience: agent
    max_words: 150
    placeholder: |
      > TODO: what this package does, who uses it, and why it exists, in one paragraph.
  - heading: Public API
    audience: agent
    max_words: 200
    placeholder: |
      > TODO: the main exports and when to use them. Link code with backticked `path:line` references.
  - heading: Files
    ownership: generated
    required: true
    placeholder: |
      _(populated by `code-wiki-okf sync` — not yet generated)_
