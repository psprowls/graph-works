# The test fixture corpus is a byte-exact contract, not source code.
#
# `edge/encoding/crlf.md` is uniformly CRLF and `edge/encoding/bom.md` carries a
# UTF-8 BOM on purpose; the vendored bundles are verbatim copies pinned to an
# upstream commit. Git's end-of-line normalization (core.autocrlf, text=auto)
# rewrites those bytes on checkout, which would break the very regressions the
# fixtures exist to guard -- and do it silently, on someone else's machine.
packages/okf-io/tests/fixtures/** -text

# okf-ext's fixtures are a byte-exact contract for the same reason okf-io's
# are: `linked/concepts/crlf.md` is uniformly CRLF on purpose, and EOL
# normalization would silently break the regression it exists to guard.
packages/okf-ext/tests/fixtures/** -text

# doc-wiki-okf's proposal corpus is a byte-exact contract for the same reason
# okf-io's and okf-ext's are: `tests/test_migrate_fixtures.py` asserts every
# migrated body is byte-identical to the fixture it came from, and Git's
# end-of-line normalization would rewrite those bytes on checkout -- silently,
# on someone else's machine -- and break the very property they exist to prove.
packages/doc-wiki-okf/tests/fixtures/** -text
