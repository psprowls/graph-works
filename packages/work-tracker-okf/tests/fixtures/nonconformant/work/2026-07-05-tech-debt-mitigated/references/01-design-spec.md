# Design spec

A real member, so the first source resolves and only its `id` is the finding.
Ignored as a concept by `IGNORE`'s `*/references/*`; still a member, which is
the distinction `targets.artifact-missing` is asking about.
