frontmatter:
  owned: [package]
  provenance: [generated, last_updated_commit, tokens, prose_refreshed_commit, prose_refresh_attempts]
sections:
  - heading: Purpose
    required: true
    placeholder: |
      > TODO: what this app does, who uses it, and why it exists, in one paragraph.
  - heading: Platform & runtime
    placeholder: |
      > TODO: the target platform(s) and runtime — e.g. web/Node, iOS, Electron.
  - heading: Routes / screens
    placeholder: |
      > TODO: a table of Route | Purpose | Auth, or the equivalent for this platform.
  - heading: Provider chain
    placeholder: |
      > TODO: the top-level providers/wrappers this app mounts, in order.
  - heading: Files
    ownership: generated
    required: true
    placeholder: |
      _(populated by `code-wiki-okf sync` — not yet generated)_
